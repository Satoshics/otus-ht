#pragma once
#include <iostream>
#include <fstream>
#include <random>
#include <string>
using std::cout, std::cin, std::endl, std::rand, std::string, std::ofstream, std::ifstream;
int level();
int rand_num();
int file_write(string nick, int attempts);
